# AppBase

Go + SQLite + Docker + React + Tailwind starter template.

## Stack
- Go (net/http)
- SQLite
- Docker Compose
- React (Vite)
- TailwindCSS

## Run Backend
docker compose up --build

## Run Frontend
cd frontend
npm install
npm run dev
